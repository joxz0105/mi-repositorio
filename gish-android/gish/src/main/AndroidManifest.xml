<?xml version="1.0" encoding="utf-8"?>
<!-- package="..." now lives in gish/build.gradle as `namespace`, and <uses-sdk>
     plus versionCode/versionName are owned by defaultConfig. Declaring any of
     them here is an error under AGP 8. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      android:installLocation="auto">

    <!-- The engine renders through OpenGL ES 1.1 (directly, or via GL4ES). -->
    <uses-feature android:glEsVersion="0x00010001" android:required="true" />

    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />

    <!-- Game data is imported into app-specific storage, so broad storage access
         is only ever needed to *read* a copy the user placed on shared storage,
         and only on the releases where that was still possible. Android 11
         removed File-API access to shared storage for non-media files, which is
         why the in-app importer exists. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application android:label="@string/app_name"
                 android:icon="@drawable/ic_launcher"
                 android:allowBackup="true"
                 android:hardwareAccelerated="true"
                 android:requestLegacyExternalStorage="true"
                 android:theme="@style/AppTheme" >

        <activity android:name=".GishLauncherActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|density"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Started only from the launcher, so it must not be exported. The old
             MAIN intent-filter here served no purpose and made the activity
             externally launchable before its settings had been initialised. -->
        <activity android:name=".GishActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:screenOrientation="sensorLandscape"
            android:resizeableActivity="false"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|density|navigation"
            android:theme="@style/GishGameTheme" >
        </activity>

        <activity android:name=".GishFilePickerActivity"
            android:exported="false"
            android:label="@string/gish_data"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout|smallestScreenSize|uiMode|density" >
        </activity>
    </application>

</manifest>
